// Owned by Person 2 — Map Track (Map Build 1/2/3).
// Scaffolded as an empty placeholder in Phase 1 so the route exists.
// Do not build map logic here in the main track.
import ComingSoon from './ComingSoon.jsx'

export default function WorksiteMap() {
  return (
    <ComingSoon
      title="Worksite Map"
      note="Full zone map, live positions, and danger-zone warnings are built on the Map Track."
    />
  )
}
