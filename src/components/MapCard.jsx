// Owned by Person 2 — Map Track.
// Scaffolded as an empty placeholder in Phase 1 so the Dashboard's map slot
// has something to render. Do not build map logic here in the main track.
export default function MapCard() {
  return null
}
